import React from "react";
import AppointmentHeader from "../AppointmentHeader/AppointmentHeader";

const Appointment = () => {
  return <AppointmentHeader></AppointmentHeader>;
};

export default Appointment;
